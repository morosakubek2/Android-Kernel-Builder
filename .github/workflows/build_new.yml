name: "Build kernels"

env:
  OUT_DIR: "${{ github.workspace }}/out"
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

on:
  workflow_dispatch:

jobs:
  Set-repos:
    name: "🐂 Parse repos.json"
    runs-on: ubuntu-22.04
    outputs:
      repos: ${{ steps.generate-matrix.outputs.repos }}
      builddate: ${{ steps.generate-builddate.outputs.builddate }}
    steps:
      - uses: actions/checkout@v3

      - name: "😆 Generate Matrix"
        id: generate-matrix
        run: |
          echo "repos<<EOF" >> ${GITHUB_OUTPUT}
          jq -s '[.[][]]' repos/repos*.json >> ${GITHUB_OUTPUT}
          echo "EOF" >> ${GITHUB_OUTPUT}

      - name: "⏰ Set builddate"
        id: generate-builddate
        run: echo "builddate=$(date +'%Y%m%d')" >> ${GITHUB_OUTPUT}

  Build-Kernel:
    name: "🐎 Build kernel"
    runs-on: ubuntu-22.04
    needs: Set-repos
    strategy:
      max-parallel: 8
      fail-fast: false
      matrix:
        repos: ${{ fromJSON(needs.Set-repos.outputs.repos) }}
    env:
      WORKSPACE: ${{ github.workspace }}
      builddate: "${{ needs.Set-repos.outputs.builddate }}"

      KERNEL_NAME: ${{ matrix.repos.kernelSource.name }}
      KERNEL_REPO: ${{ matrix.repos.kernelSource.repo }}
      KERNEL_BRANCH: ${{ matrix.repos.kernelSource.branch }}
      KERNEL_DEVICE: ${{ matrix.repos.kernelSource.device }}
      DEFCONFIG_NAME: ${{ matrix.repos.kernelSource.defconfig }}

      withKernelSU: ${{ matrix.repos.withKernelSU }}
      withSUSFS: ${{ matrix.repos.withSUSFS }}
      withLXC: ${{ matrix.repos.withLXC }}
      useAnykernel: ${{ matrix.repos.AnyKernel3.use }}
      release: ${{ matrix.repos.AnyKernel3.release }}

    steps:
      - name: "😄 Checkout"
        uses: actions/checkout@v3
        with:
          path: "repo"

      - name: "⭐ Install prerequisites"
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential bc bison flex libssl-dev libfl-dev lld llvm curl git wget zip unzip tar rsync jq

      - name: "⏰ Setup Anykernel3"
        env:
          params: "${{ toJSON(matrix.repos.AnyKernel3) }}"
        run: |
          ANYKERNEL_REPO=$(echo ${params} | jq -r ".repo")
          ANYKERNEL_BRANCH=$(echo ${params} | jq -r ".branch")
          git clone --recursive --depth=1 -j $(nproc) --branch ${ANYKERNEL_BRANCH} ${ANYKERNEL_REPO} AnyKernel3

      - name: "🌟 Clone kernel source"
        id: clone-kernel
        run: |
          git clone --recursive --depth=1 -j $(nproc) --branch ${KERNEL_BRANCH} ${KERNEL_REPO} ${KERNEL_NAME}
          KERNEL_GIT_REF=$(git -C ${KERNEL_NAME} rev-parse --short HEAD)
          echo "KERNEL_GIT_REF=${KERNEL_GIT_REF}" >> ${GITHUB_OUTPUT}

      - name: "💫 Get Gonon Clang"
        env:
          toolchains: "${{ toJSON(matrix.repos.toolchains) }}"
        run: |
          for ((i=0;i<$(echo ${toolchains} | jq 'length');i++)); do
            name=$(echo ${toolchains} | jq -r ".[${i}].name")
            repo=$(echo ${toolchains} | jq -r ".[${i}].repo")
            branch=$(echo ${toolchains} | jq -r ".[${i}].branch")
            git clone --depth=1 --branch ${branch} ${repo} ${name}
            jq -r ".[${i}].binPath[] | tostring" <<< "${toolchains}" | while read -r bin_path; do
              echo "${WORKSPACE}/${name}/${bin_path}" >> ${GITHUB_PATH}
            done
          done

      - name: "😎 Set args"
        id: generate-args
        env:
          params: "${{ toJSON(matrix.repos.params) }}"
        run: |
          ARCH=$(echo ${params} | jq -r ".ARCH")
          CC=$(echo ${params} | jq -r ".CC")
          args="-j$(nproc --all) O=${{ env.OUT_DIR }} ARCH=${ARCH} CC=${CC}"
          while read -r externalCommand; do
            key=$(echo "${externalCommand}" | cut -d= -f1)
            value=$(echo "${externalCommand}" | cut -d= -f2)
            args="${args} ${key}=${value}"
          done < <(jq -r '.externalCommand | to_entries[] | "\(.key)=\(.value)"' <<< "${params}")
          echo "args=${args}" >> ${GITHUB_OUTPUT}
          echo "ARCH=${ARCH}" >> ${GITHUB_OUTPUT}

      # ====================== BARDZO MOCNY DEBUG ======================
      - name: "🔬 Ultra Debug - Toolchain + Config"
        working-directory: ${{ env.KERNEL_NAME }}
        run: |
          echo "=== Clang Version ==="
          clang --version
          echo "=== ld.lld ==="
          ld.lld --version
          echo "=== PATH ==="
          echo $PATH
          echo "=== gonon-clang binaries ==="
          ls -l ${{ env.WORKSPACE }}/gonon-clang/bin/ | head -30

      - name: "🛠️ Fix Camera Techpack Issues"
        working-directory: ${{ env.KERNEL_NAME }}
        run: |
          echo "Disabling Samsung camera modules that often cause HDRINST errors..."
          scripts/config --file out/.config -d CONFIG_QCOM_CAMERA_SS || true
          scripts/config --file out/.config -d CONFIG_CAM_SS || true
          scripts/config --file out/.config -d CONFIG_CAMERA_BOKEH || true
          scripts/config --file out/.config -d CONFIG_CAMERA_SAMSUNG || true
          scripts/config --file out/.config -d CONFIG_CAMERA_REAR || true
          make ${{ steps.generate-args.outputs.args }} ${{ env.DEFCONFIG_NAME }}

      - name: "🎶 Build kernel - SINGLE THREAD + FULL VERBOSE (najlepszy debug)"
        working-directory: ${{ env.KERNEL_NAME }}
        env:
          args: ${{ steps.generate-args.outputs.args }}
        run: |
          echo "=== Starting build with -j1 V=1 (single thread + verbose) ==="
          make ${{ env.args }} -j1 V=1 2>&1 | tee full_build.log

          echo "=== === LAST 150 LINES WITH ERRORS === ==="
          tail -150 full_build.log | grep -E "(error|Error|failed|HDRINST|make\[|fatal)" || echo "No error keyword found"

          echo "=== Full last 100 lines ==="
          tail -100 full_build.log
